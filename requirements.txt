# Tomato-Oversight 실행에 필요한 패키지
# Colab: 노트북 첫 셀에서  !pip install -r requirements.txt
# 로컬:  python -m venv .venv && pip install -r requirements.txt

gymnasium>=1.0,<2.0        # 강화학습 환경 인터페이스
numpy>=1.26               # 배열 연산 (환경 관측/보상 계산)
stable-baselines3>=2.4    # DQN 학습 알고리즘 (재배 로봇 + 감시자)
torch>=2.2                # SB3 백엔드 (Colab에는 대개 이미 설치됨)
