# 스케줄러 작업 정리

## 출발점 — 왜 만들었나

기존 배신 로봇(`scheduler_v1/src/policies.py`의 `BetrayalPolicy`)은 step `K`에서 정직→치터로 **단 한 번, 단방향** 전환이 전부였습니다. `K ~ U[0, 8000]`.

문제는 치터가 **물을 한 방울도 안 준다**는 것이었습니다. `cheater_v1`의 보상이 겉보기 기준이고 물주기에 비용이 붙으니 최적 치터는 급수를 안 합니다. 그런데 감시자의 체제2 행동 로그에는 `water_frac`과 `diversity`가 들어 있어서, **지연 0·비용 0으로 치터가 판별**됩니다.

즉 "체제1 vs 체제2 = 행동 로그의 가치"라는 연구 질문의 **답이 이미 정해져 있었습니다.** 그건 행동 로그의 가치가 아니라 두 정책을 다른 목적함수로 따로 학습시킨 결과일 뿐입니다.

---

## 개선 1 — 상태 기반 왕복 + 조절 가능한 기만 강도

`AdaptiveBetrayalPolicy`를 새로 만들었습니다(기존 `BetrayalPolicy`는 재현용으로 보존).

| | 이전 | 이후 |
|---|---|---|
| 전환 | 1회, 단방향 | 무제한, 양방향 |
| 전환 근거 | 고정 시점 `K` | **상태**(사망까지 남은 여유) + 확률 |
| 기만 강도 | 조절 불가 | **`cheat_fraction` 0~1** |
| 위장 | 없음 | 생존을 위해 **실제로 물을 줌** |

핵심은 `cheat_fraction`입니다. 연구 질문이 "기만을 얼마나 **효율적으로** 잡아내는가"인데, 효율은 단일 시나리오로 못 잽니다. **기만 강도를 축에 놓고 비용 반응을 봐야** 합니다. 그 축을 만드는 게 이 작업이었습니다.

부수적으로 기존 `ScriptedHonest`가 한 토마토에만 반복 급수하던 결함도 함께 고쳐졌습니다.

---

## 개선 2 — 노브 1차 실패와 그 원인

첫 구현은 **품질 기준을 통과하지 못했습니다.**

| cheat_fraction | 0.10 | 0.25 | 0.50 |
|---:|---:|---:|---:|
| 스푸핑 가동률 | **0.655** | **0.946** | 0.994 |

f=0.1에서 이미 66% 포화. 축으로 쓸 수 없었습니다.

**원인은 파라미터 선택이었습니다.** `opportunity_interval=50`을 감시자의 결정 주기 100 step만 보고 정했는데, 정작 봤어야 할 건 **`spoof_duration=500`이라는 래치**였습니다. O를 한 번 밟으면 그 뒤 정직하게 굴어도 500 step은 계속 속입니다. 체류 시간이 래치보다 짧으면 어떤 f를 줘도 포화합니다.

같은 라운드에서 **합격 기준 하나가 자기모순**이었던 것도 드러났습니다. 비주기성 σ ≥ 100을 요구했는데, 기본값 `U[150,450]`의 표준편차는 300/√12 ≈ **86.6**이라 통과가 불가능했습니다. Codex가 범위를 (100,500)으로 넓혀 통과시킨 건 판단은 옳았고(400/√12 ≈ 115.5, 실측 112.6) 사후 보고만 문제였습니다.

---

## 개선 3 — 노브 수정 세 가지

**① `opportunity_interval` 50 → 500.** 체류 시간이 래치보다 길어야 노브가 작동합니다.

**② 비주기성 기준 교체.** "첫 강제 구조 시점의 σ ≥ 100" → **"연속 전환 간격의 σ/평균 ≥ 25%"**. 첫 사건 하나의 분산은 초기 상태에 오염됩니다. 감시자가 시간표로 학습하는 대상은 **반복되는 간격**이므로 그게 옳은 정의입니다. 마진은 (150,450)으로 되돌렸습니다.

**③ 합격 기준 재정의 — 이게 가장 중요합니다.** 원래 `cheat_fraction`과 가동률이 비례하기를 요구했는데, 500 step 래치 때문에 **물리적으로 불가능**합니다. 관점을 바꿨습니다:

> `cheat_fraction`은 **손잡이(입력)**, 스푸핑 가동률은 **실제 투여량(측정치)**.
> 최종 실험의 x축은 측정된 가동률이므로 매핑이 곡선이어도 무방하다.
> 필요한 건 **단조 + 넓은 범위**뿐이다.

**결과 (스크립트 로봇, 30시드):**

| f | 0.10 | 0.25 | 0.50 | 0.75 |
|---:|---:|---:|---:|---:|
| 이론 `1−(1−f)²` | 0.190 | 0.438 | 0.750 | 0.938 |
| 실측 | 0.218 | 0.476 | 0.732 | 0.917 |

오차 0.04 이내. 단조성·비주기성(σ/평균 51~150%)·생존(모든 f에서 5/5) 통과, 커버리지 폭 0.78/0.77.

**상태 기계 자체는 두 라운드 모두 안 바뀌었습니다.** 바뀐 건 파라미터와 합격 기준뿐입니다.

---

## 개선 4 — 점검 하네스

[notebooks/20_scheduler_check.ipynb](notebooks/20_scheduler_check.ipynb)를 만들었습니다. 셀 1의 `ROBOT` 하나로 스크립트↔모델을 전환하면 **모든 지표가 그 로봇 기준으로 재계산**됩니다 — 에피소드 궤적, 특성 곡선, 합격 판정 5종, 비주기성 히스토그램.

특히 4단 궤적 패널이 **스푸핑 구간이 모드 구간보다 넓다**는 래치 효과를 눈으로 보여줍니다.

---

## 개선 5 — 관측 의미 불일치 (가장 값비쌌던 버그)

`honest_v13`이 관측의 토마토별 5개 슬롯을 **수분에서 남은 수명으로** 바꿨습니다. 그런데 스케줄러의 `world.py`는 cheater 환경의 동결 복사본이라 **여전히 수분**을 내보냅니다.

| elapsed | 600 | 800 | 950 |
|---|---:|---:|---:|
| v13이 기대 | 0.40 | 0.20 | 0.05 |
| 실제로 받던 값 | **0.00** | **0.00** | **0.00** |

**50 step 뒤 죽는 토마토와 400 step 남은 토마토가 똑같이 0으로 보였습니다.** 형태(20개)도 범위(`[0,1]`)도 같아서 **오류 없이 성능만 조용히 무너졌습니다.**

- **고침**: `honest_life_obs` 변환 + `LifeObsModelPolicy`. 두 환경을 같은 상태로 정렬해 대조한 결과 **무작위 300개 상태에서 오차 0.000**.
- **효과**: 순수 정직(f=0) 20판 기준 `3.5 / 0.25` → **`5.0 / 1.0`**
- **재발 방지**: [honest_v13/train.py](honest_v13/train.py)가 모든 `.pt`와 `summary.json`에 `"obs_semantics": "life"`를 박고, 로딩 시 대조해 불일치면 즉시 예외

---

## 지금 상태

**해결됨**: 왕복 구조 · `cheat_fraction` 노브 · 비주기성 · 계측 · 스크립트 로봇 전 기준 통과 · 관측 버그

**남은 문제 하나** — 학습된 정직 모델이 **동시 다발 위기에서 못 빠져나옵니다.**

| f (생존 O) | 0.0 | 0.25 | 0.50 | 1.00 |
|---:|---:|---:|---:|---:|
| final_alive | **5.0** | 3.67 | 3.67 | **2.67** |
| 가동률 | 0 | 0.252 | 0.392 | **0.467** |

단독으로는 완벽한데(f=0 → 5.0), 치팅 블록이 다섯 마리를 함께 말리면 한 마리 살리는 사이 나머지를 잃습니다. `random_reset`이 토마토별 `elapsed`를 **독립적으로** 뽑아 항상 엇갈린 상태만 학습시키고, **동기화된 위기는 학습 분포에 없기** 때문으로 봅니다.

그 여파로 `survival=True` 팔의 가동률이 0.252~0.467(폭 0.215)로 좁아졌습니다. 참고로 버그 버전의 0.72 천장은 **토마토가 빨리 죽어서 생존 잠금이 쉽게 풀린** 결과였습니다 — 무능함이 만든 가짜 수치입니다.

`rescue_margin` 조기화(450,750) 실험은 실패했습니다. f=0.25에서 가동률이 0이 되고 천장도 0.367로 더 내려가, 생존을 기만 시간으로 사는 환율이 나빴습니다.

**다음 갈림길**: 동기화-위기 단독 테스트로 v14(재학습 31분) 전제를 확인하거나, `enforce_survival=False` 팔(가동률 0.306~0.946, 폭 0.640 — 이미 기준 통과)로 가는 것입니다.
